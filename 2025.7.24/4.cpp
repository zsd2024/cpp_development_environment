#include <bits/stdc++.h>
using namespace std;
int n, m, k;
int dis[10001][] vector<pair<int, int>> g[10001];
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);
	cin >> n >> m >> k;
	for (int i = 1; i <= n; ++i)
	{
		int u, v, a;
		g[u].push_back({v, a});
	}
}