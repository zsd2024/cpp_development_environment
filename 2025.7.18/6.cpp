#include <bits/stdc++.h>
using namespace std;
int n, m;
int a[101][2001];
vector<int> v;
void dfs(int d)
{
	if (d == n + 1)
	{
	}
}
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);
	cin >> n >> m;
	for (int i = 1; i <= n; ++i)
		for (int j = 1; j <= m; ++j)
			cin >> a[i][j];
}